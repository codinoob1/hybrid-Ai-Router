// src/prober.ts
var SIMD_VALIDATION_MODULE = new Uint8Array([
  0,
  97,
  115,
  109,
  1,
  0,
  0,
  0,
  1,
  4,
  1,
  96,
  0,
  0,
  3,
  2,
  1,
  0,
  10,
  9,
  1,
  7,
  0,
  65,
  0,
  253,
  15,
  26,
  11
]);
function getNavigator() {
  return globalThis.navigator;
}
function asFiniteNonNegativeNumber(value) {
  return typeof value === "number" && Number.isFinite(value) && value >= 0 ? value : null;
}
function detectWasmCapabilities(crossOriginIsolated) {
  if (typeof WebAssembly === "undefined") return { supported: false, simd: false, threads: false };
  const simd = WebAssembly.validate(SIMD_VALIDATION_MODULE);
  let threads = false;
  if (crossOriginIsolated && typeof SharedArrayBuffer !== "undefined") {
    try {
      new WebAssembly.Memory({ initial: 1, maximum: 1, shared: true });
      threads = true;
    } catch {
    }
  }
  return { supported: true, simd, threads };
}
async function detectWebGpuCapabilities(browserNavigator) {
  const unavailable = {
    available: false,
    fallbackAdapter: null,
    maxBufferSize: null,
    maxStorageBufferBindingSize: null
  };
  if (!browserNavigator?.gpu) return unavailable;
  try {
    const adapter = await browserNavigator.gpu.requestAdapter();
    if (!adapter) return unavailable;
    return {
      available: true,
      fallbackAdapter: adapter.isFallbackAdapter ?? false,
      maxBufferSize: asFiniteNonNegativeNumber(adapter.limits?.maxBufferSize),
      maxStorageBufferBindingSize: asFiniteNonNegativeNumber(adapter.limits?.maxStorageBufferBindingSize)
    };
  } catch {
    return unavailable;
  }
}
function detectConnection(browserNavigator) {
  const connection = browserNavigator?.connection;
  const effectiveType = connection?.effectiveType ?? null;
  const downlinkMbps = asFiniteNonNegativeNumber(connection?.downlink);
  const saveData = typeof connection?.saveData === "boolean" ? connection.saveData : null;
  const slowConnection = effectiveType === "slow-2g" || effectiveType === "2g";
  return {
    effectiveType,
    downlinkMbps,
    saveData,
    modelDownloadRecommended: !saveData && !slowConnection && (downlinkMbps === null || downlinkMbps >= 1.5)
  };
}
function selectTier(input) {
  const { webgpu, wasm, deviceMemoryGiB, cpuCores, reasons } = input;
  const insufficientMemory = deviceMemoryGiB !== null && deviceMemoryGiB < 4;
  const modestMemory = deviceMemoryGiB !== null && deviceMemoryGiB < 8;
  const insufficientCpu = cpuCores !== null && cpuCores < 2;
  if (webgpu.available && !webgpu.fallbackAdapter && !insufficientMemory && !insufficientCpu) {
    if (!modestMemory) {
      reasons.push("WebGPU is available with sufficient device-memory and CPU hints for standard local models.");
      return "standard";
    }
    reasons.push("WebGPU is available, but reported device memory is below 8 GiB; using the light tier.");
  }
  if (wasm.simd && !insufficientMemory && !insufficientCpu) {
    reasons.push("WebAssembly SIMD is available for lightweight local models.");
    return "light";
  }
  if (insufficientMemory) reasons.push("Reported device memory is below the 4 GiB light-tier minimum.");
  if (insufficientCpu) reasons.push("Reported CPU concurrency is below the 2-core light-tier minimum.");
  if (!wasm.simd) reasons.push("WebAssembly SIMD is unavailable.");
  if (webgpu.fallbackAdapter) reasons.push("Only a fallback WebGPU adapter was provided.");
  reasons.push("Using cloud-only mode.");
  return "none";
}
async function probeDevice(options = {}) {
  const browserNavigator = options.navigator ?? getNavigator();
  const crossOriginIsolated = options.crossOriginIsolated ?? globalThis.crossOriginIsolated ?? false;
  const reasons = [];
  const webgpu = await detectWebGpuCapabilities(browserNavigator);
  const wasm = detectWasmCapabilities(crossOriginIsolated);
  const connection = detectConnection(browserNavigator);
  const deviceMemoryGiB = asFiniteNonNegativeNumber(browserNavigator?.deviceMemory);
  const cpuCores = asFiniteNonNegativeNumber(browserNavigator?.hardwareConcurrency);
  const tier = selectTier({ webgpu, wasm, deviceMemoryGiB, cpuCores, reasons });
  if (!connection.modelDownloadRecommended) {
    reasons.push("Connection settings indicate that downloading a local model should be deferred.");
  }
  if (wasm.threads) reasons.push("WASM threads are usable in this cross-origin-isolated context.");
  return { tier, webgpu, wasm, deviceMemoryGiB, cpuCores, connection, reasons };
}

// src/registry.ts
var MEBIBYTE = 1024 * 1024;
var REGISTRY = [
  {
    id: "qwen2.5-0.5b-q4f16",
    label: "Qwen2.5 0.5B",
    params: "0.5B",
    runtime: "wasm",
    minTier: "light",
    downloadSizeMB: 380,
    minDeviceMemoryGiB: 4
  },
  {
    id: "phi3-mini-q4f16",
    label: "Phi-3-mini",
    params: "3.8B",
    runtime: "webgpu",
    minTier: "standard",
    downloadSizeMB: 2100,
    minDeviceMemoryGiB: 8,
    requiredMaxBufferSize: 512 * MEBIBYTE,
    requiredStorageBufferBindingSize: 256 * MEBIBYTE
  }
  //More models to be added in the future 
];
var TIER_RANK = { none: 0, light: 1, standard: 2 };
function canRun(probe, model) {
  if (TIER_RANK[probe.tier] < TIER_RANK[model.minTier]) return false;
  if (model.minDeviceMemoryGiB !== void 0 && probe.deviceMemoryGiB !== null && probe.deviceMemoryGiB < model.minDeviceMemoryGiB) {
    return false;
  }
  if (model.runtime === "webgpu") {
    if (!probe.webgpu.available || probe.webgpu.fallbackAdapter) return false;
    if (model.requiredMaxBufferSize !== void 0 && (probe.webgpu.maxBufferSize === null || probe.webgpu.maxBufferSize < model.requiredMaxBufferSize)) {
      return false;
    }
    if (model.requiredStorageBufferBindingSize !== void 0 && (probe.webgpu.maxStorageBufferBindingSize === null || probe.webgpu.maxStorageBufferBindingSize < model.requiredStorageBufferBindingSize)) {
      return false;
    }
  }
  if (model.runtime === "wasm") {
    if (!probe.wasm.supported || !probe.wasm.simd) return false;
    if (model.requiresThreads && !probe.wasm.threads) return false;
  }
  if (probe.connection.saveData) return false;
  if (!probe.connection.modelDownloadRecommended && model.downloadSizeMB > 500) return false;
  return true;
}
function pickModel(probe) {
  const candidates = REGISTRY.filter((model) => canRun(probe, model));
  return candidates.reduce(
    (best, candidate) => !best || candidate.downloadSizeMB > best.downloadSizeMB ? candidate : best,
    null
  );
}

// src/quality.ts
var MAX_LATENCY_MS = 1e3;
var MIN_LENGTH = 10;
var REFUSAL_PATTERNS = [/^i (can'?t|cannot)\b/i, /^sorry,? i/i];
function scoreResponse(text, context) {
  const trimmed = text.trim();
  if (trimmed.length < MIN_LENGTH) {
    return {
      reason: "Response too short",
      quality: 0,
      context,
      pass: false
    };
  }
  ;
  if (context.latency > MAX_LATENCY_MS) {
    return {
      reason: "Response too slow",
      quality: 0,
      context,
      pass: false
    };
  }
  if (REFUSAL_PATTERNS.some((p) => p.test(trimmed))) {
    return { pass: false, reason: "refusal pattern matched", quality: 0, context };
  }
  if (hasRepeatedLoop(trimmed)) {
    return {
      reason: "Repeated loop detected",
      quality: 0,
      context,
      pass: false
    };
  }
  return {
    quality: 1,
    context,
    pass: true
  };
}
function hasRepeatedLoop(text) {
  const words = text.split(/\s+/);
  if (words.length < 12) return false;
  const lastSix = words.slice(-6).join(" ");
  const prevSix = words.slice(-12, -6).join(" ");
  return lastSix === prevSix;
}

// src/orchestrator.ts
function createRouter(config) {
  let probe = null;
  let loadedRuntime = null;
  let loadedModelId = null;
  async function init() {
    probe = await probeDevice();
    const model = pickModel(probe);
    const runtime = model && config.adapters[model.runtime];
    if (!runtime) {
      loadedRuntime = null;
      loadedModelId = null;
      return;
    }
    await runtime.load(model.id);
    loadedRuntime = runtime;
    loadedModelId = model.id;
  }
  async function generate(prompt) {
    if (!loadedRuntime) {
      return { text: await config.cloud.generate(prompt), source: "cloud" };
    }
    const start = performance.now();
    const localText = await loadedRuntime.generate(prompt);
    const latencyMs = performance.now() - start;
    const score = scoreResponse(localText, { latency: latencyMs });
    if (score.pass) {
      return { text: localText, source: "local", modelId: loadedModelId };
    }
    return { text: await config.cloud.generate(prompt), source: "cloud" };
  }
  return { init, generate };
}
export {
  REGISTRY,
  canRun,
  createRouter,
  pickModel,
  probeDevice,
  scoreResponse
};
