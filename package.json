{
  "name": "hybrid-ai-router",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.19.0",
  "scripts": {
    "build": "pnpm --recursive --if-present run build",
    "test": "pnpm --recursive --if-present run test",
    "dev:demo": "vite examples/vanilla-demo"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.0",
    "eslint": "^9.0.0",
    "tsup": "^8.5.1",
    "typescript": "^5.5.0",
    "vite": "^7.0.0",
    "vitest": "^2.0.0"
  }
}
